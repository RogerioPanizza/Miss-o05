# Microatividade 02

class bubbleSort:
    def arr(self, array):
        for i in range(len(array)):
            for j in range(0, len(array) - i - 1):
                if j > j+1:
                    var = array[j+1]
         
        return array.sort()